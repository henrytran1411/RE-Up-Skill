/**
 * Derived, not stored: ON_BENCH whenever the employee has no currentProject.
 * See employees.service.ts for the mapping.
 */
export enum EmployeeStatus {
  ON_PROJECT = 'on_project',
  ON_BENCH = 'on_bench',
}
