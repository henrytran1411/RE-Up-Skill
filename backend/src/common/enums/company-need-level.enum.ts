/** How much the company currently needs this skill — set by HR/Admin/Tech Lead on the skill catalog. */
export enum CompanyNeedLevel {
  VERY_NEEDED = 'very_needed',
  NORMALLY = 'normally',
  DONT_NEED = 'dont_need',
}
